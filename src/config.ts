/**
 * Application configuration.
 * Previously kept as a separate file to allow editing without rebuild.
 * With Vite"s HMR, this is no longer needed as a separate file.
 */
export const config = {} as const;
